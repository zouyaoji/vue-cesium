import { semiMajorAxis, semiMinorAxis, height, extrudedHeight, rotation, stRotation, granularity } from './allProps'

export default {
  mixins: [semiMajorAxis, semiMinorAxis, height, extrudedHeight, rotation, stRotation, granularity]
}
